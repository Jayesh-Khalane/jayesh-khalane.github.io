# Portfolio Site

This folder is the GitHub Pages source for the portfolio.

Keep shared layout and component styles in `css/style.css`, keep page-specific content in `pages/`, and store downloadable assets under `assets/`.
